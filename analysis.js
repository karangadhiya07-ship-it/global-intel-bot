const json=(data,status=200,maxAge=300)=>new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":`public, max-age=${maxAge}`}});const safeFetch=async(url)=>{const r=await fetch(url,{headers:{"user-agent":"GlobalIntelTimes/3.0"}});if(!r.ok)throw new Error(`Fetch failed ${r.status}`);return r.json();};export async function onRequestGet({request}){const symbol=(new URL(request.url).searchParams.get("symbol")||"AAPL").toUpperCase();return json({symbol,summary:`${symbol} analysis combines price trend, volume, related news, macro risk, sector strength and market sentiment.`,risks:["Interest rates","Inflation","Geopolitical events","Earnings surprises","Liquidity"],opportunities:["Momentum breakout","Sector leadership","Positive news flow","Institutional accumulation"]})}
