import {json} from './_utils.js';export async function onRequestGet({request}){const symbol=(new URL(request.url).searchParams.get('symbol')||'AAPL').toUpperCase();return json({symbol,aiAnalysis:`${symbol} analysis uses trend, volume, sentiment, macro risk and latest news flow.`,support:'API dependent',resistance:'API dependent',trend:'Neutral',sentiment:'Mixed',buyScore:56,sellScore:44})}
