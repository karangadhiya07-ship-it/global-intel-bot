const json=(data,status=200,maxAge=300)=>new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":`public, max-age=${maxAge}`}});const safeFetch=async(url)=>{const r=await fetch(url,{headers:{"user-agent":"GlobalIntelTimes/3.0"}});if(!r.ok)throw new Error(`Fetch failed ${r.status}`);return r.json();};export async function onRequestGet({request}){const input=(new URL(request.url).searchParams.get("coin")||"bitcoin").toLowerCase();const map={btc:"bitcoin",eth:"ethereum",sol:"solana",bitcoin:"bitcoin",ethereum:"ethereum"};const id=map[input]||input;try{const d=await safeFetch(`https://api.coingecko.com/api/v3/simple/price?ids=${encodeURIComponent(id)}&vs_currencies=usd&include_24hr_change=true&include_market_cap=true&include_24hr_vol=true`);const row=d[id]||{},change=row.usd_24h_change||0;return json({name:id,price:row.usd||0,marketCap:row.usd_market_cap||0,volume:row.usd_24h_vol||0,change:Number(change.toFixed(2)),sentiment:change>=0?"Bullish momentum":"Risk-off sentiment"})}catch(e){return json({name:id,price:0,sentiment:"Crypto unavailable"})}}
